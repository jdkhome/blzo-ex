jar {
    baseName = "blzo-core"
}
dependencies {
    // 公共依赖
    compile project(":blzo-common")

    compile project(":blzo-ex-usignin")
    compile project(":blzo-ex-version")
    compile project(":blzo-ex-risk")
    //compile project(":blzo-ex-redission")
}
