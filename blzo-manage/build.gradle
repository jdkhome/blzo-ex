jar {
    baseName = "blzo-manage"
}
dependencies {
    // 公共依赖
    compile project(":blzo-common")

    compile project(":blzo-ex-authj")
    
    compile project(":blzo-ex-utils")

    // thymeleaf
    compile('org.springframework.boot:spring-boot-starter-thymeleaf')

}
