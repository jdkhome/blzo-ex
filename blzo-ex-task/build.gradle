jar {
    baseName = "blzo-ex-task"
}

dependencies {
    // redis
    compile('org.springframework.boot:spring-boot-starter-data-redis')

    compile project(":blzo-ex-basic")

    compile project(":blzo-ex-redission")
}